# MongoDB Configuration
# Option 1: MongoDB Local (recommandé pour le développement)
MONGODB_URI=mongodb://localhost:27017/raspberry-pi-manager

# Option 2: MongoDB avec Docker
# MONGODB_URI=mongodb://localhost:27017/raspberry-pi-manager

# Option 3: MongoDB Atlas (cloud)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority
# IMPORTANT: Si votre mot de passe contient des caractères spéciaux, encodez-les en URL
# Exemple: & devient %26, @ devient %40, etc.

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRE=7d

# Serveur
PORT=5000
NODE_ENV=development

# Frontend URL (pour CORS) - Utiliser http://localhost:3000 pour le développement local
FRONTEND_URL=http://localhost:3000

# Socket.IO URL - Utiliser http://localhost:3000 pour le développement local
SOCKET_URL=http://localhost:3000
